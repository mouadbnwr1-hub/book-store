<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Theme.Book" parent="Theme.Material3.Light.NoActionBar">
        <!-- Colors -->
        <item name="colorPrimary">@color/gris_base</item>
        <item name="colorPrimaryContainer">@color/light_beige</item>
        <item name="colorOnPrimary">@color/soft_gray</item>

        <item name="colorSecondary">@color/light_gold</item>
        <item name="colorSecondaryContainer">@color/off_white</item>

        <item name="android:colorBackground">@color/off_white</item>
        <item name="colorSurface">@color/off_white</item>
        <item name="colorOnBackground">@color/soft_gray</item>

        <item name="android:navigationBarColor">@color/off_white</item>
        <item name="android:statusBarColor">@color/gris_base</item>

        <!-- Typography / Shape (optional) -->
        <item name="android:textColor">@color/soft_gray</item>
    </style>

</resources>
